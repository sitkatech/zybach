apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "vegarenderservice.fullname" . }}-pdb
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      app: {{ include "vegarenderservice.fullname" . }}